# Real implementations

This folder is reserved for small adapters or reduced specimens from real projects.

Do not add synthetic variants just to make the library look larger.

Current source implementations that motivated the pattern:

- `blackmath88/feedback-samzuercher`
  - structured JSON offboard containing thesis, research and sources.
- `blackmath88/change-management-platform-demo`
  - generated task prompts from live case/module state;
  - copy + open provider flow;
  - deterministic case brief → Markdown clipboard export.

When a third project proves a genuinely different boundary, add the smallest specimen needed to explain it here.
